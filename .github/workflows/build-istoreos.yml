name: "Build iStoreOS Installer ISO"

on:
  workflow_dispatch:

# 防止同一时间运行多个相同工作流
concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

# 设置超时时间（4小时）
timeout-minutes: 240

permissions:
  contents: write  # 发布release需要写权限

jobs:
  build-release:
    name: "Build and Release"
    runs-on: "ubuntu-22.04"

    steps:

      - name: "Checking out git repository"
        uses: actions/checkout@v4
      
      - name: Set executable permissions
        run: |
          chmod +x ${{ github.workspace }}/istoreos.sh
          chmod +x ${{ github.workspace }}/supportFiles/istoreos/build.sh

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Cache Docker layers
        uses: actions/cache@v4
        with:
          path: |
            ~/.cache/docker
            /tmp/.buildx-cache
            .docker-cache
          key: ${{ runner.os }}-docker-${{ hashFiles('supportFiles/istoreos/build.sh', 'supportFiles/istoreos/**/*') }}
          restore-keys: |
            ${{ runner.os }}-docker-

      - name: Cache Debian packages
        uses: actions/cache@v4
        with:
          path: ~/.cache/apt
          key: ${{ runner.os }}-apt-${{ hashFiles('supportFiles/istoreos/**/*.sh') }}
          restore-keys: |
            ${{ runner.os }}-apt-

      - name: "Build iStoreOS Installer ISO"
        run: |
          set -e  # 遇到错误立即退出
          echo "开始构建 iStoreOS 安装器..."
          ./istoreos.sh
          echo "构建完成！"
      
      - name: "Publish"
        uses: softprops/action-gh-release@v2.2.1
        with:
          tag_name: istoreos.iso
          body_path: ${{ github.workspace }}/supportFiles/istoreos/info.md
          files: |
            output/istoreos-*.iso
          token: ${{ secrets.GITHUB_TOKEN }}
          append_body: true
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
